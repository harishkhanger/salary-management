services:
  mysql:
    image: mysql:8.4
    environment:
      MYSQL_DATABASE: salary_mgmt
      MYSQL_USER: salary
      MYSQL_PASSWORD: salary
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql
volumes:
  mysql_data:
